class Protagonista extends Element {
    constructor(x, y, ancho, alto, imagen, posicionX, posicionY, indice) {
      super(x, y, ancho, alto, imagen, posicionX, posicionY, indice);
    }
  
    moverArriba() {
      // mover hacia arriba
    }
  
    moverAbajo() {
      // mover hacia abajo
    }
  
    moverIzquierda() {
      // mover hacia la izquierda
    }
  
    moverDerecha() {
      // mover hacia la derecha
    }
  
    detectarColisiones() {
      // detectar colisiones con enemigos y recompensas
    }
  }
