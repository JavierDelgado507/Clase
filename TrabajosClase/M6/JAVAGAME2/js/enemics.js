// Ejemplo de un enemigo que hereda de la clase Enemigo
class Enemigo1 extends Enemigo {
    constructor(x, y, width, height) {
      super(x, y, width, height);
    }
  
    // Método para ejecutar el ataque al colisionar con el protagonista
    atacar(protagonista) {
      // Lógica para realizar el ataque y aplicar penalizaciones al protagonista
    }
  }
  
  // Ejemplo de otro enemigo que hereda de la clase Enemigo
  class Enemigo2 extends Enemigo {
    constructor(x, y, width, height) {
      super(x, y, width, height);
    }
  
    // Método para ejecutar el ataque al colisionar con el protagonista
    atacar(protagonista) {
      // Lógica para realizar el ataque y aplicar penalizaciones al protagonista
    }
  }
  
  // Ejemplo de otro enemigo que hereda de la clase Enemigo
  class Enemigo3 extends Enemigo {
    constructor(x, y, width, height) {
      super(x, y, width, height);
    }
  
    // Método para ejecutar el ataque al colisionar con el protagonista
    atacar(protagonista) {
      // Lógica para realizar el ataque y aplicar penalizaciones al protagonista
    }
  }
  