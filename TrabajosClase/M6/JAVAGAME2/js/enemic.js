class Enemigo extends Element {
  constructor(x, y, width, height) {
    super(x, y, width, height);
  }

  // Métodos para manejar el movimiento autónomo del enemigo buscando al protagonista
  moverEnBuscaProtagonista(protagonista) {
    // Lógica para moverse hacia el protagonista en busca de atraparlo
  }
}
